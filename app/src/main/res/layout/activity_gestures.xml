<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ui_background"
    android:orientation="vertical">

    <!-- Immersive Header -->
    <LinearLayout
        android:id="@+id/header_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="?attr/actionBarSize"
        android:background="@color/ui_header_background"
        android:fitsSystemWindows="true"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <ImageView
            android:id="@+id/btn_back"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="?selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:src="@drawable/ic_arrow_back"
            app:tint="@color/ui_header_text" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="?attr/actionBarSize"
            android:layout_marginStart="32dp"
            android:gravity="center_vertical"
            android:text="@string/header_gestures"
            android:textColor="@color/ui_header_text"
            android:textSize="20sp"
            android:textStyle="normal" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="42dp">

            <!-- ================= LEFT TOP ================= -->
            <include layout="@layout/item_gesture_zone_left"
                android:id="@+id/zone_left_top" />

            <!-- ================= LEFT MID ================= -->
            <include layout="@layout/item_gesture_zone_left"
                android:id="@+id/zone_left_mid" />

            <!-- ================= LEFT BOTTOM ================= -->
            <include layout="@layout/item_gesture_zone_left"
                android:id="@+id/zone_left_bottom" />

            <!-- ================= LEFT FULL ================= -->
            <include layout="@layout/item_gesture_zone_left"
                android:id="@+id/zone_left_full" />

            <!-- ================= RIGHT TOP ================= -->
            <include layout="@layout/item_gesture_zone_right"
                android:id="@+id/zone_right_top" />

            <!-- ================= RIGHT MID ================= -->
            <include layout="@layout/item_gesture_zone_right"
                android:id="@+id/zone_right_mid" />

            <!-- ================= RIGHT BOTTOM ================= -->
            <include layout="@layout/item_gesture_zone_right"
                android:id="@+id/zone_right_bottom" />

            <!-- ================= RIGHT FULL ================= -->
            <include layout="@layout/item_gesture_zone_right"
                android:id="@+id/zone_right_full" />

            <!-- ================= TOP LEFT ================= -->
            <include layout="@layout/item_gesture_zone_top"
                android:id="@+id/zone_top_left" />

            <!-- ================= TOP MID ================= -->
            <include layout="@layout/item_gesture_zone_top"
                android:id="@+id/zone_top_mid" />

            <!-- ================= TOP RIGHT ================= -->
            <include layout="@layout/item_gesture_zone_top"
                android:id="@+id/zone_top_right" />

            <!-- ================= TOP FULL ================= -->
            <include layout="@layout/item_gesture_zone_top"
                android:id="@+id/zone_top_full" />

            <!-- ================= BOTTOM LEFT ================= -->
            <include layout="@layout/item_gesture_zone_bottom"
                android:id="@+id/zone_bottom_left" />

            <!-- ================= BOTTOM MID ================= -->
            <include layout="@layout/item_gesture_zone_bottom"
                android:id="@+id/zone_bottom_mid" />

            <!-- ================= BOTTOM RIGHT ================= -->
            <include layout="@layout/item_gesture_zone_bottom"
                android:id="@+id/zone_bottom_right" />

            <!-- ================= BOTTOM FULL ================= -->
            <include layout="@layout/item_gesture_zone_bottom"
                android:id="@+id/zone_bottom_full" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>
